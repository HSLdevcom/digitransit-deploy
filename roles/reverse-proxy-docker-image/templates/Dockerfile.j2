FROM nginx

MAINTAINER Reittiopas version: 0.1

RUN mkdir -p {{reverse_proxy_root}}

ADD proxy.conf /etc/nginx/nginx.conf

ADD index.html {{reverse_proxy_root}}/index.html

ADD img {{reverse_proxy_root}}/img

ADD css {{reverse_proxy_root}}/css

WORKDIR /etc/nginx

CMD ["nginx"]
